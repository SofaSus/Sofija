#include <iostream>
#include <vector>

using namespace std;
int main()
{
	vector<int>laiks{-1, -3, 0, 1, -2, 0, 13, -2, -22, 1};
	int i = 0, a = 0; 

		do {
			if (laiks[i] > 0) {
		
				a++;
			};
			i++;
		} while (i < laiks.size());
		cout << a;
	return 0;
}
